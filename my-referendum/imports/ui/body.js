import { Template } from 'meteor/templating' // Meteor's templating engine
import { Votes } from '../api/votes' // This is the database itself
import '../util/utilityFunctions' // Utility functions for manipulating what's displayed
import './body.html' // The HTML file that is paired with the JS file you're looking at now
import './helpers.js' // Helpers to passing info between the JS and the HTML
import './events.js' // Functions to handle DOM events
